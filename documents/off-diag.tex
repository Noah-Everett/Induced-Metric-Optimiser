\documentclass{article}

\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{mathtools}
\usepackage[most]{tcolorbox}
\usepackage{bbm}
\usepackage{hyperref}

\newcommand{\loss}{\mathcal{L}}
\newcommand{\Id}{\mathbbm{1}}
\newcommand{\vecb}[1]{\boldsymbol{#1}}

\title{Off-Diagonal Inverse}
\date{\today}

\begin{document}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Problem Statement}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
Given a model with $N$ parameters $\theta(t)=\{\theta^i(t)\}_{i=1}^N$ and a loss function 
$\loss:\mathbb{R}^N\to\mathbb{R}$, 
our goal is to find the most optimal parameter update $\delta \theta$ at each time step $t$.
That is, in gradient descent we have $$
    \frac{d \theta^i}{dt} = - g^{ij} \frac{\partial \loss}{\partial \theta^j} 
    \implies 
    \delta \theta^i \approx - \eta g^{ij} \frac{\partial \loss}{\partial \theta^j}
$$ where $\eta\in\mathbb{R}_{>0}$ is the learning rate and $g^{ij}$ is the inverse of the induced metric on the parameter space.



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Review of Diagonal Metric Update}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
The original ambient metric in the paper is $$
    h = \begin{pmatrix} \gamma & \vec{0} \\ \vec{0}^\top & 1 \end{pmatrix}\in\mathbb{R}^{(N+1)\times(N+1)}
$$ where $\gamma\in\mathbb{R}^{N\times N}$.
What we really care about is the induced metric on the parameter space, which is given by the pullback of $h$ under the embedding 
$\phi:\mathbb{R}^N\to\mathbb{R}^{N+1}$ defined by 
$\phi(\theta^1,\ldots,\theta^N) = (\theta^1,\ldots,\theta^N,f(\theta^1,\ldots,\theta^N))$ 
where $f:\mathbb{R}^N\to\mathbb{R}$ is some scalar function which we take to be the loss function $\loss$.
The induced metric $g$ on the parameter space is given by $$ 
    g_{ij} 
    = h_{\alpha\beta} \frac{\partial \phi^\alpha}{\partial \theta^i} \frac{\partial \phi^\beta}{\partial \theta^j} 
    = \gamma_{ij} + \frac{\partial \loss}{\partial \theta^i} \frac{\partial \loss}{\partial \theta^j}.
$$ Then, to calculate the parameter updates $\delta \theta^i$ we need $g^{ij}$, the inverse of $g_{ij}$.
We can compute this inverse using the Sherman-Morrison-Woodbury formula:
\begin{tcolorbox}[colback=gray!8, colframe=gray!22, breakable, title=\textbf{Sherman-Morrison-Woodbury Formula}, coltitle=black]
    Let $A\in\mathbb{R}^{n\times n}$ be an invertible matrix, and let $U,V\in\mathbb{R}^{n\times k}$ be matrices. 
    Then, if $C = A + U V^\top$ is invertible, we have $$C^{-1} = A^{-1} - A^{-1} U (I_k + V^\top A^{-1} U)^{-1} V^\top A^{-1}.$$
\end{tcolorbox}
\noindent This gives us $$ 
    g^{ij} 
    = \gamma^{ij} - \frac{\gamma^{ik}\gamma^{jl} \frac{\partial \loss}{\partial \theta^k} \frac{\partial \loss}{\partial \theta^l}}{1 + \gamma^{mn} \frac{\partial \loss}{\partial \theta^m} \frac{\partial \loss}{\partial \theta^n}}
    = \gamma^{ij} - \frac{l^i l^j}{1+l_k l^k} 
$$ where $$
    l_i \coloneqq \frac{\partial\loss}{\partial \theta^i} \implies l^i = \gamma^{ij} l_j.
$$ Thus, the parameter updates are given by
\begin{align*}
    \delta \theta^i =& -\eta \left( \gamma^{ij} - \frac{l^i l^j}{1+l_k l^k} \right) l_j \\
    =& -\eta l^i \left( 1 - \frac{l_j l^j}{1+l_k l^k} \right) \\
    =& -\eta l^i \left( \frac{1}{1+l_k l^k} \right).
\end{align*}

So to summarize, (1) we start with an ambient metric $h$ which induces a metric $g$ on the parameter space, 
(2) we compute the inverse $g^{ij}$, and 
(3) we use $g^{ij}$ to compute the parameter updates $\delta \theta^i$.



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Off-Diagonal Metric Update}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
Now, we consider a more general ambient bilinear form of the form $$
    h = \begin{pmatrix} \gamma & \vec{a} \\ \vec{b}^\top & 1 \end{pmatrix}\in\mathbb{R}^{(N+1)\times(N+1)}
$$ where $\vec{a},\vec{b}\in\mathbb{R}^N$.
Intuitively, the vectors $\vec{a}$ and $\vec{b}$ introduce off-diagonal couplings between the parameter space and the loss function dimension.
In general, if $\vec{a}\neq \vec{b}$ this $h$ is not symmetric, so here we simply treat it as a preconditioning matrix rather than a Riemannian metric.
The induced bilinear form on the parameter space is then given by
\begin{align*}
    g_{ij} 
    &= h_{\alpha\beta} \frac{\partial \phi^\alpha}{\partial \theta^i} \frac{\partial \phi^\beta}{\partial \theta^j} \\
    &= \gamma_{ij} + a_i \frac{\partial \loss}{\partial \theta^j} + b_j \frac{\partial \loss}{\partial \theta^i} + \frac{\partial \loss}{\partial \theta^i} \frac{\partial \loss}{\partial \theta^j} \\
    &= \gamma_{ij} + a_i l_j + b_j l_i + l_i l_j.
\end{align*}
To compute the inverse $g^{ij}$, we notice that $g$ can be expressed as a rank-2 update to $\gamma$, 
that is for $U=[a, l], V=[l, b + l]\in\mathbb{R}^{N\times 2}$ we have
\begin{align*}
    \gamma_{ij} + U_{ik} V_{jk} 
    &= \gamma_{ij} + a_i l_j + (b_j + l_j) l_i \\
    &= \gamma_{ij} + a_i l_j + b_j l_i + l_i l_j \\
    &= g_{ij}.
\end{align*}
Thus, we can apply the Sherman-Morrison-Woodbury formula again to get
\begin{align*}
    g^{ij} 
    &= \gamma^{ij} - \gamma^{ik} U_{k\alpha} \Big(\delta_{\alpha\beta} + V_{m\alpha} \gamma^{mn} U_{n\beta}\Big)^{-1} V_{l\beta} \gamma^{lj} \\
    % &= \gamma^{ij} - \gamma^{ik} (a_k, l_k) \begin {pmatrix} 
    %         \delta_{11} + V_{m1} \gamma^{mn} U_{n1} & \delta_{12} + V_{m1} \gamma^{mn} U_{n2} \\ 
    %         \delta_{21} + V_{m2} \gamma^{mn} U_{n1} & \delta_{22} + V_{m2} \gamma^{mn} U_{n2} 
    %    \end{pmatrix}^{-1} \begin{pmatrix} l_n \\ b_n + l_n \end{pmatrix} \gamma^{nj} \\
    &= \gamma^{ij} - \gamma^{ik} (a_k, l_k) \begin{pmatrix}
            1 + l_m \gamma^{mn} a_n & l_m \gamma^{mn} l_n \\
            (b_m + l_m) \gamma^{mn} a_n & 1 + (b_m + l_m) \gamma^{mn} l_n
       \end{pmatrix}^{-1} \begin{pmatrix} l_n \\ b_n + l_n \end{pmatrix} \gamma^{nj} \\
    &= \gamma^{ij} - \gamma^{ik} (a_k, l_k) \begin{pmatrix}
            1 + l_n a^n & l_n l^n \\
            b_n a^n + l_n a^n & 1 + b_n l^n + l_n l^n
       \end{pmatrix}^{-1} \begin{pmatrix} l_n \\ b_n + l_n \end{pmatrix} \gamma^{nj}.
\end{align*} 
For convenience we define the following scalar contractions: $$
    c_{al} \coloneqq a_n l^n,\quad
    c_{ll} \coloneqq l_n l^n,\quad
    c_{ba} \coloneqq b_n a^n,\quad
    c_{bl} \coloneqq b_n l^n.
$$ Thus, we have
\begin{align*}
    g^{ij} 
    &= \gamma^{ij} - \gamma^{ik} (a_k, l_k) \begin{pmatrix}
            1 + c_{al} & c_{ll} \\
            c_{ba} + c_{al} & 1 + c_{bl} + c_{ll}
       \end{pmatrix}^{-1} \begin{pmatrix} l_n \\ b_n + l_n \end{pmatrix} \gamma^{nj} \\
    &= \gamma^{ij} - \gamma^{ik} \frac{1}{D} (a_k, l_k) \begin{pmatrix}
            1 + c_{bl} + c_{ll} & -c_{ll} \\
            -(c_{ba} + c_{al}) & 1 + c_{al}
       \end{pmatrix} \begin{pmatrix} l_n \\ b_n + l_n \end{pmatrix} \gamma^{nj} \\
    &= \gamma^{ij} - \gamma^{ik} \frac{1}{D} (a_k, l_k) \begin{pmatrix}
            (1 + c_{bl} + c_{ll}) l_n - c_{ll} (b_n + l_n) \\
            -(c_{ba} + c_{al}) l_n + (1 + c_{al}) (b_n + l_n)
       \end{pmatrix} \gamma^{nj} \\
    &= \gamma^{ij} - \gamma^{ik} \frac{1}{D} \left[ a_k \big( (1 + c_{bl}) l_n - c_{ll} b_n \big) + l_k \big( -(c_{ba} + c_{al}) l_n + (1 + c_{al}) (b_n + l_n) \big) \right] \gamma^{nj}
\end{align*}
where
\begin{align*}
    D 
    &= \det
    \begin{pmatrix}
        1 + c_{al} & c_{ll} \\
        c_{ba} + c_{al} & 1 + c_{bl} + c_{ll}
    \end{pmatrix} \\
    &= (1 + c_{al})(1 + c_{bl} + c_{ll}) - c_{ll} (c_{ba} + c_{al}) \\
    &= 1 + c_{al} + c_{bl} + c_{al} c_{bl} + c_{ll} - c_{ll} c_{ba}.
\end{align*}
\begin{tcolorbox}[colback=gray!8, colframe=gray!22, breakable, title=\textbf{Quick Sanity Check}, coltitle=black]
    If we set $\vec{a}=\vec{0}$ and $\vec{b}=\vec{0}$, we should recover the diagonal metric case.
    In this case, $U=[0,l]$ and $V=[l,l]$, so $$
    c_{al} = 0,\quad c_{ba} = 0,\quad c_{bl} = 0,\quad c_{ll} = l_n l^n,
    $$ and hence $$
    D = 1 + c_{ll}.
    $$
    Then
    \begin{align*}
        g^{ij}
        &= \gamma^{ij} - \gamma^{ik} \frac{1}{1+c_{ll}} (0, l_k) \begin{pmatrix}
                (1 + 0) l_n - c_{ll} l_n \\
                -(0 + 0) l_n + (1 + 0) (0 + l_n)
           \end{pmatrix} \gamma^{nj} \\
        &= \gamma^{ij} - \gamma^{ik} \frac{1}{1+c_{ll}} (0, l_k) \begin{pmatrix}
                (1 - c_{ll}) l_n \\
                l_n
           \end{pmatrix} \gamma^{nj} \\
        &= \gamma^{ij} - \gamma^{ik} \frac{1}{1+c_{ll}} l_k l_n \gamma^{nj} \\
        &= \gamma^{ij} - \frac{l^i l^j}{1 + l_k l^k},
    \end{align*}
    which matches our previous result.
\end{tcolorbox}
\noindent Thus, the parameter updates are given by
\begin{align*}
    \delta\theta^i
    &= -\eta\, g^{ij} l_j \\
    &= -\eta\left[ \gamma^{ij} - \gamma^{ik}\frac{1}{D}\Big( 
            a_k\!\left((1+c_{bl})l_n - c_{ll} b_n\right)
            + l_k\!\left(-(c_{ba}+c_{al})l_n + (1+c_{al})(b_n + l_n)\right)
        \Big)\gamma^{nj} \right] l_j \\
    &= -\eta\left[ l^i - \frac{1}{D}\gamma^{ik}\Big(
            a_k\!\left((1+c_{bl})l_n - c_{ll}b_n\right)
            + l_k\!\left(-(c_{ba}+c_{al})l_n + (1+c_{al})(b_n+l_n)\right)
        \Big)l^n \right] \\
    &= -\eta\left[ l^i - \frac{1}{D}\Big(
            a^i\,c_{ll}
            + l^i\big(-(c_{ba}+c_{al})c_{ll} + (1+c_{al})(c_{bl}+c_{ll})\big)
        \Big) \right] \\
    &= -\frac{\eta}{D}\left[ D\,l^i - a^i c_{ll} - l^i\big(-(c_{ba}+c_{al})c_{ll} + (1+c_{al})(c_{bl}+c_{ll})\big) \right] \\
    &= -\frac{\eta}{D}\left[ (1 + c_{al})\,l^i - a^i c_{ll} \right] \\
    &= -\eta\left[ \frac{1 + c_{al}}{D}\,l^i - \frac{c_{ll}}{D}\,a^i \right].
\end{align*}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Implementation}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
The off-diagonal optimizers have been implemented in 
\href{https://github.com/Noah-Everett/Induced-Metric-Optimiser/blob/f6dbb04c8fe5605c415b4aa3de1706dc4bbab842/optimisers/jax_offdiag.py}{optimisers/jax\_offdiag.py} 
and 
\href{https://github.com/Noah-Everett/Induced-Metric-Optimiser/blob/f6dbb04c8fe5605c415b4aa3de1706dc4bbab842/optimisers/torch_offdiag.py}{optimisers/torch\_offdiag.py}.

\subsection*{General Update Direction}
The off-diagonal update rules derived above have been implemented in both JAX and PyTorch.
In both implementations, the goal is to realise the update $$
    \delta\theta^i = -\eta\, g^{ij} r_j,
$$ where $r_j$ is the update direction.
In ordinary gradient descent one would simply take $r_j = \partial_j \loss$,
but we can also test other choices such as momentum-based updates $r_j = m_j$ where $m_j$
is an exponentially-smoothed average of past gradients (the usual ``momentum'' vector).
This separation between the geometric direction $l_i$ appearing in the metric
and the update direction $r_i$ allows the optimizer to use the metric
to shape the descent direction in a more flexible way.
Note: the results discussed later will only take $r_j = l_j$.

\subsection*{Update Computational Cost}
Recall that the induced bilinear form is $$
    g_{ij}
    = \gamma_{ij}
      + \left(a_i l_j + l_i b_j + l_i l_j\right).
$$ Taking $\gamma_{ij}\to\gamma(\Id_N)_{ij}$
(that is, we take the $\gamma$ matrix to be a scalar multiple of the identity matrix so from now on $\gamma$ is a scalar), 
adding a hyperparameter $\xi$ to control the strength of the induced correction, and using matrix notation, we have that $g$ is now $$
    G = \gamma\Id_N + \xi\bigl(\vecb{a}\vecb{l}^\top + \vecb{l}\vecb{b}^\top + \vecb{l}\vecb{l}^\top\bigr).
$$ We can write this as $$
    G = \gamma\,\Id_N + \xi\,U V^\top,
    \qquad U = (\vecb{a},\, \vecb{l}),
    \qquad V = (\vecb{l},\, \vecb{b} + \vecb{l}).
$$ Using the inverse of $G$ we found above, we have $$
    G^{-1} \vecb{r}
    = \frac{1}{\gamma} \vecb{r} - \frac{\xi}{\gamma^2} U \left(\Id_2 + \frac{\xi}{\gamma} V^\top U\right)^{-1} V^\top \vecb{r}.
$$ Thus, the contraction $G^{-1} \vecb{r}$ has computational cost $O(N)$ 
since it only involves the inversion of a $2\times 2$ matrix 
and scalar products never producing a full $N\times N$ matrix.

\subsection*{Modes for $\vecb{l}$, $\vecb{a}$, and $\vecb{b}$}
The optimiser provides several ways to choose the geometric vectors
$\vecb{l}$, $\vecb{a}$, and $\vecb{b}$ appearing in the induced off-diagonal bilinear form.

\paragraph{The base direction $\vecb{l}$:}
The vector $\vecb{l}$ encodes the direction in parameter space along which the loss coordinate of the ambient space stretches.
Two choices are provided:
\begin{itemize}
    \item \textbf{Gradient mode}: $$
        \vecb{l}_i = \frac{\partial \loss}{\partial \theta^i}.
    $$ This reproduces the behaviour of the original diagonal construction,
    where the metric is shaped by the instantaneous slope of the loss.

    \item \textbf{Momentum mode}: $$
        \vecb{l}_i = \vecb{m}_i,
    $$ where $\vecb{m}_i$ is the bias-corrected exponential moving average of gradients.
    In this mode the metric is influenced not only by the current gradient,
    but by the accumulated direction of travel over recent steps.
\end{itemize}
Either choice may be paired with $\vecb{r}=\vecb{l}$ or $\vecb{r}=\vecb{m}$ depending on
whether the user wishes to precondition the raw gradient or the
momentum direction.

\paragraph{The off-diagonal vectors $\vecb{a}$ and $\vecb{b}$:}
The vectors $\vecb{a}$ and $\vecb{b}$ determine how the parameter coordinates couple to the
loss coordinate in the ambient bilinear form.
Several built-in modes allow for quick experimentation:
\begin{itemize}
    \item \textbf{Zero mode}: 
    $\vecb{a}_i=0$ or $\vecb{b}_i=0$.
    This eliminates one branch of the off-diagonal coupling.

    \item \textbf{Gradient mode}: 
    $\vecb{a}_i$ or $\vecb{b}_i$ is aligned with
    $\partial_i \loss$, emphasising the local slope of the loss surface.

    \item \textbf{Momentum mode}: 
    $\vecb{a}_i$ or $\vecb{b}_i$ is aligned with the
    momentum vector $\vecb{m}_i$, incorporating information from recent history.

    \item \textbf{Parameter mode}: 
    $\vecb{a}_i=\theta^i$ or $\vecb{b}_i=\theta^i$,
    coupling the geometry directly to the position in parameter space.

    \item \textbf{Same-as-$a$ mode}: 
    $\vecb{b}_i=\vecb{a}_i$, producing a symmetric
    off-diagonal structure.
\end{itemize}
For full generality, the implementations also accept user-specified choices
for $\vecb{a}$ and $\vecb{b}$ or user-provided functions that construct these vectors
dynamically at each optimisation step.

Overall, the off-diagonal framework provides a direct and computationally
efficient way to explore a richer class of induced metrics.
The various modes for $\vecb{l}$, $\vecb{a}$, and $\vecb{b}$ allow the user to interpolate
between purely gradient-based behavior, momentum-informed geometry,
and more exotic couplings determined by the structure of the model or task.
















%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Results}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
So far I have only tested them on the small examples.
The results are in the following commit files:
\begin{itemize}
    \item \href{https://github.com/Noah-Everett/Induced-Metric-Optimiser/blob/9bca2c00747351b45c7248c8fd24ec1660155c39/analysis/small_examples.ipynb}{HyperbandPruner + RandomSampler}
    \item \href{https://github.com/Noah-Everett/Induced-Metric-Optimiser/blob/d49b510bb12559c9952f84be37bdb6f3d295a743/analysis/small_examples.ipynb}{SuccessiveHalvingPruner + RandomSampler}
    \item \href{https://github.com/Noah-Everett/Induced-Metric-Optimiser/blob/116da11f0ac0c299b67f8376fba8a5c0ecce7af4/analysis/small_examples.ipynb}{NopPruner + RandomSampler}
\end{itemize}
In general the results are not promising as seen in the following figures.

\begin{figure}[h]
    \centering
    \begin{minipage}{0.72\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../images/Beale_116da11.png}
    \end{minipage}
    \begin{minipage}{0.72\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../images/Rosenbrock_116da11.png}
    \end{minipage}
    \begin{minipage}{0.72\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../images/Himmeiblau_116da11.png}
    \end{minipage}
    \begin{minipage}{0.72\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../images/Ackley_116da11.png}
    \end{minipage}
    \begin{minipage}{0.72\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../images/Rastrigin_116da11.png}
    \end{minipage}
    \caption{
        All small example results for \href{https://github.com/Noah-Everett/Induced-Metric-Optimiser/blob/116da11f0ac0c299b67f8376fba8a5c0ecce7af4/analysis/small_examples.ipynb}{NopPruner + RandomSampler}, 
        that is no early pruning (so train all models to completion or max 1000 epochs).
        Each plot shows the best hyper parameter configuration for each optimizer.
        Note that the naming scheme for the off-diagonal optimizers: \texttt{custom\_sgd\_offdiag\_\{mode a\}\_\{mode b\}}.
    }
\end{figure}


\end{document}
